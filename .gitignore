# ---- Rust build artifacts ----
/target/
**/target/
**/*.rs.bk
*.pdb

# Note: Cargo.lock IS committed. This is an application crate
# shipping 7 binaries — a committed lockfile gives reproducible
# builds for end users and is the standard guidance from the Cargo
# book.

# ---- Documentation & benchmark artifacts ----
/target/doc/
/criterion/
/perf.data*
*.flamegraph
*.prof
*.heapdump

# ---- Claude Code context files (NEVER commit) ----
# CLAUDE.md provides cross-session context to the Claude agent. It lives
# at the PARENT of this repo (rvt-recon-2026-04-19/CLAUDE.md), not
# inside the repo. This entry is a defensive guard in case anyone
# copies or symlinks it here.
CLAUDE.md
.claude/*.local.md
.claude/*.local.json
# Claude Code spawns isolated git worktrees here when agents run —
# never commit them (they're embedded git repos on throwaway branches).
.claude/worktrees/

# ---- Local draft / personal content (NEVER commit) ----
# Outreach emails, internal decision memos, personal notes,
# anything that shouldn't be readable by strangers on the
# internet.
docs/outreach/
docs/announcement-draft.md
docs/drafts/
docs/_local/
drafts/
*.draft.md
*.private.md

# ---- Log files ----
*.log
/logs/

# ---- Environment / local overrides ----
.env
.env.*
!.env.example
*.local.toml
*.local.yaml
config.local.*

# ---- Editor / IDE ----
.vscode/
.idea/
.nvim/
.zed/
*.swp
*.swo
*.swn
*~
\#*\#
.\#*

# ---- OS cruft ----
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
ehthumbs.db
ehthumbs_vista.db
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ---- Generated / backup ----
*.orig
*.rej
*.bak
*.old
*.tmp

# ---- Python (in case we add tooling) ----
__pycache__/
*.pyc
*.pyo
*.pyd
# Native extension produced by `maturin develop` (editable install)
# lands in python/rvt/ — it's a build artifact, never committed.
*.so
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# ---- Node (in case we add JS tooling) ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---- Test-corpus RFA files (if ever vendored locally) ----
# The phi-ag/rvt LFS corpus lives outside this repo; if someone
# drops sample files into a local samples/ dir for testing,
# keep them out of git.
/samples/
/test-files/
*.rvt
*.rfa
*.rte
*.rft
# Lane Three: redistributable synthetic fixtures under corpus/ are
# intentionally committed (Apache-2.0 gen-fixture output + license
# sidecars). Negate the blanket *.rvt ignore for that tree only.
!corpus/
!corpus/**
!corpus/**/*.rvt
!docs/**/*.rvt.md
# Redistributable, license-documented fixtures committed under tests/
!tests/fixtures/families/
!tests/fixtures/families/**
!tests/fixtures/families/**/*.rfa
!tests/fixtures/families/**/*.license.json

# wasm-pack output (built in CI, not committed)
pkg/
viewer/pkg/
viewer/node_modules/
viewer/dist/
viewer/public/demos/*
!viewer/public/demos/.gitkeep

_corpus_candidates/
_corpus/
_project_corpus/

# Revit-hosted oracle runner output (owned synthetic fixtures + observations)
tools/oracle/out/

# Any local cargo target dir (e.g. target-msrv/ used for MSRV checks) — never commit build output.
/target*/
# Native Revit/CAD source files are local qualification inputs, never source.
*.rvt
*.rfa
*.ifc
*.dwg
*.nwc
*.nwd
*.nwf
